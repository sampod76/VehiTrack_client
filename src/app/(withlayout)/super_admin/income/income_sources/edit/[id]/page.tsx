
import React from "react";

const IncomeSourcesEdit = ({ params }: { params: { id: string } }) => {

  return (
    <div>
      <h1> IncomeSources {params.id}</h1>
    </div>
  );
};

export default IncomeSourcesEdit;
